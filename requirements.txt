# Core dependencies
opencv-python==4.8.1.78
mediapipe==0.10.9
numpy==1.24.3

# Media control (cross-platform)
pyautogui==0.9.54

# Windows-specific audio control (required for Windows volume control)
pycaw==20231129
comtypes==1.2.0

# Linux media control
# Install separately on Linux: sudo apt-get install playerctl

# macOS media control is built-in via osascript

# Future Spotify integration (when API available)
# spotipy==2.23.0

# Optional: Better keyboard simulation
# pynput==1.7.6